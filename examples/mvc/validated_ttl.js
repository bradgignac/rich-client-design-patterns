(function (exports) {

  var ValidatedTTL = function (domain) {
    // this.domain = domain;
    // this.controller = new CreateDomainFormController(this, domain);

    this.render = function (container) {

    };
  };

  this.ValidatedTTL = ValidatedTTL;

}(window));
